<div class="col-md-3 col-sm-4 col-xs-12">
  <div class="thumbnail">
    <%= image_tag perk.image.url(:thumb) %>
    <div class="caption">
      <h3><%= perk.name %></h3>
      <h5><%= perk.item_category.name %></h5>
      <p><%= perk.description %></p>
      <p>
        <a href="#" class="btn btn-default" role="button">info</a>
        <button type="button" class="btn btn-success pull-right" data-toggle="modal" data-target="#myModal">Add perk</button>
      </p>
    </div>
  </div>
</div>